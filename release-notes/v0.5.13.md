## 修复内容

- **更准确的线缆端点检测。** CC 端点（由 MagSafe 端口使用）现在仅在传输类型字段中出现时映射，而不是在组件名称字段中。这防止了名称中带有"CC"的未来线缆或设备被错误识别为线缆 e-marker。感谢 @0x687931 发现此问题。

## 安装

`brew upgrade --cask whatcable`，或从此发布页面下载 `WhatCable.zip` 并将新的 `.app` 拖到 `/Applications`。
